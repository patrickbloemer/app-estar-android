<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ti.estar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.MainActivity" />
        <activity android:name=".view.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.CriarContaActivity" />
        <activity android:name=".view.CadVeiculoActivity" />
        <activity android:name=".view.EstacionarActivity" />
        <activity android:name=".view.EstacionamentoConfirmadoActivity" />
        <activity android:name=".view.item_list_veiculo_layout" />
        <activity android:name=".util.ArrayAdapterEstacionamentosAtivos" />
        <activity android:name=".view.ComprarSaldoActivity" />
    </application>

</manifest>